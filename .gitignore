# Secrets / local config
.env
.env.*
!env.example
watchdog.secrets

# Runtime data (event log volume mount target for local runs)
/data/

# Build artifacts
/dns-modem-watchdog
*.exe
*.test
*.out

# Internal planning / tooling (not for the published repo)
openspec/
.atl/

# Stray compiled command binaries
/verify
/cmd/**/verify
/sshprobe

# Overnight observer artifacts
nightwatch.bin
nightwatch.log
nightwatch.sh

# Day watchdog artifacts
daywatch.bin
daywatch.log
events.jsonl

# Measurement-only
Dockerfile.measure
